bin/
obj/
*.user
.vs/

# Build output. The publish folder in particular holds a ~70 MB single-file executable, which has no
# business in the history — it is a release asset, not source.
out/
publish/

# MSIX build output — a 65 MB package and a throwaway test certificate.
out-msix/

# Release assets: a ~68 MB single-file exe, a ~64 MB zip, and the loose folder build they come from.
# These belong on a GitHub release, not in the history — 130 MB that every clone would pay for ever after.
out-release/

TestResults/
